var scrollCounter = 0;

window.onscroll = function(e) {
    // print "false" if direction is down and "true" if up
    console.log(this.scrollY);
    if(this.scrollY > 500 && this.scrollY < 1000){
        
    }
  }